---
description: '@v-md/ui' 的文档规范
globs: libs/ui/src/**/*,websites/doc/docs/components/**/*.md,websites/doc/docs/demo/**/*.vue
alwaysApply: false
---
# 📋 概述

本文档定义了 '@v-md/ui' 组件库的文档编写、维护和质量控制标准。

---

# 📁 目录结构规范

## 文档目录结构
```
websites/doc/docs/
├── components/                # 组件文档目录
│   ├── index.md              # 组件列表页
│   ├── basic/                # 基础组件
│   │   ├── index.md        # 基础组件列表页
│   │   ├── button.md        # 按钮组件文档
│   │   ├── input.md         # 输入框组件文档
│   │   └── ...
│   ├── form/                 # 表单组件
│   │   ├── index.md        # 表单组件列表页
│   │   ├── form.md          # 表单组件文档
│   │   ├── checkbox.md      # 复选框组件文档
│   │   └── ...
│   └── ...
└── demo/                     # 组件示例目录
    ├── basic/               # 基础组件示例
    │   ├── button/         # 按钮组件示例
    │   │   ├── button-basic.vue
    │   │   ├── button-size.vue
    │   │   └── ...
    │   ├── input/          # 输入框组件示例
    │   │   ├── input-basic.vue
    │   │   ├── input-password.vue
    │   │   └── ...
    │   └── ...
    ├── form/               # 表单组件示例
    │   ├── form/          # 表单组件示例
    │   │   ├── form-basic.vue
    │   │   ├── form-validation.vue
    │   │   └── ...
    │   └── ...
    └── ...
```

## 组件分类体系
组件按功能分为以下几类：
1. **基础组件（basic）**：按钮、输入框、图标等
2. **表单组件（form）**：表单、单选框、复选框等
3. **数据展示组件（data）**：表格、列表、树形控件等
4. **导航组件（navigation）**：菜单、标签页、面包屑等
5. **反馈组件（feedback）**：对话框、提示、加载等
6. **其他组件（other）**：其他功能性组件

---

# 📝 文档编写规范

## 命名规范

### 文档文件命名
- 组件文档文件：`组件名.md`
- 组件示例文件：`示例名.vue`
- **组件名必须与组件库(`libs/ui/src`)中的组件名目录名称一致**

### 示例文件命名
- **基础示例**：`组件名-basic.vue`
- **功能示例**：`组件名-功能名.vue`
- **组合示例**：`组件名-combination.vue`

### 子组件处理规则
- 子组件（如`menu-item`）不单独创建文档，作为父组件（如`menu`）的一部分展示

## 文档结构模板

````markdown
# 组件名称

组件简介，说明组件的主要功能和用途。

## 基础用法

描述基础用法，展示组件的核心功能

:::demo
../demo/组件分类/组件名/组件名-basic.vue
:::

## 功能1

描述功能1的用途和使用场景

:::demo
../demo/组件分类/组件名/组件名-功能1.vue
:::

## 功能2

描述功能2的用途和使用场景

:::demo
../demo/组件分类/组件名/组件名-功能2.vue
:::

## API

### 组件名称 Props
| 属性名 | 类型 | 默认值 | 是否必填 | 说明 |
|--------|------|--------|----------|------|
| prop1  | type | value  | true/false| desc |

### 组件名称 Events
| 事件名 | 参数 | 说明 |
|--------|------|------|
| event1 | args | desc |

### 组件名称 Slots
| 插槽名 | 作用域参数 | 说明 |
|--------|------------|------|
| slot1  | { data }   | desc |

### 组件名称 Model
| 属性名 | 类型 | 默认值 | 是否必填 | 说明 |
|--------|------|--------|----------|------|
| model1 | type | value  | true/false| desc |

### 组件名称 Expose
| 属性名 | 类型 | 默认值 | 是否必填 | 说明 |
|--------|------|--------|----------|------|
| expose1 | type | value  | true/false| desc |

### 组件名称 CSS 变量
| 变量名 | 默认值 | 说明 |
|--------|--------|------|
| --vmd-组件名-属性名 | value | desc |
````

---

# 💻 示例代码规范

## 代码结构标准

```vue
<script setup lang="ts">
// 从 '@v-md/ui' 中手动引入组件，此处以 Icon 为例
import { Icon } from '@v-md/ui'
</script>

<template>
  <!-- 示例模板，在其中使用组件 -->
</template>

<style scoped>
/* 示例样式 */
</style>
```

## 代码编写要求

### 基础要求
- ✅ 组件必须从 `@v-md/ui` 中手动引入
- ✅ 每个示例专注于展示一个功能点
- ✅ 代码简洁明了，避免复杂的业务逻辑
- ✅ 包含必要的注释说明
- ✅ 使用 TypeScript
- ✅ 样式使用 scoped

### 样式规范
- ✅ 使用 CSS 变量进行样式定制
- ✅ 遵循 BEM 命名规范
- ⚠️ **组件样式应直接应用到组件本身，而非外层容器**
- ⚠️ **避免过度嵌套的样式选择器**
- ⚠️ **组件props与外部样式冲突时，优先使用组件props**

### 资源文件使用
只允许以下两种方式：
1. **公共资源**：位于 `websites/doc/docs/public/` 目录，使用绝对路径引入
   - 示例：`/images/arrow-left.png`
   - 禁止引入不存在的资源文件
2. **内联资源**：直接使用 base64 编码字符串
   - 示例：`data:image/svg+xml;base64,PHN2ZyB4bWxuc...`

---

# 🔍 API文档规范

## 数据来源原则
> **所有API文档必须忠于源码，绝对不能编造**

### Props、Events、Slots、Model、Expose
- **数据来源**：`libs/ui/src/组件名/types/组件名.ts`
- **一致性要求**：属性名、类型必须与源码完全一致
- **说明优先级**：优先使用源码注释，无注释时清晰描述用途和限制
- **存在性检查**：源码中不存在的部分，文档中不展示

## 类型格式化规范
- **类型格式**：所有类型必须使用反引号包围
  - ✅ 正确：`string`、`number`、`boolean`、`Array<string>`
  - ❌ 错误：string、number、boolean、Array<string>
- **复杂类型**：对象类型和联合类型同样使用反引号
  - ✅ 正确：`{ name: string; age: number }`、`'small' | 'medium' | 'large'`
  - ❌ 错误：{ name: string; age: number }、'small' | 'medium' | 'large'

### CSS变量
- **数据来源**：`libs/ui/src/组件名/style/var.scss`
- **一致性要求**：变量名、默认值必须与源码完全一致
- **说明要求**：清晰描述变量的用途和影响范围

---

# 🔄 文档维护流程

## 新增组件流程
1. **创建文档**：在 `docs/components/对应分类/` 创建组件文档
2. **创建示例**：在 `docs/demo/对应分类/组件名/` 创建示例文件
3. **更新索引**：在对应分类的 `index.md` 中添加组件链接
4. **验证分类**：确保组件分类准确，目录结构一致

## 更新组件流程
1. **同步更新**：文档与代码变更同步进行
2. **示例维护**：新增或更新对应示例
3. **API检查**：确保API文档与源码一致
4. **功能验证**：验证所有示例代码可正常运行

## 分类调整流程
1. **目录迁移**：同时调整文档和示例的目录结构
2. **索引更新**：更新相关的索引文件
3. **链接检查**：确保所有内部链接正确
4. **决策记录**：记录分类调整的原因和依据

---

# ✅ 质量控制标准

## 示例质量检查

### 内容一致性
- ✅ 示例描述准确反映代码实际功能
- ✅ 示例标题简洁明了，直接说明功能点
- ❌ 避免模糊或过于宽泛的描述

### 完整性验证
- ✅ 基础示例展示组件核心功能
- ✅ 功能示例覆盖主要使用场景
- ✅ 每个示例独立完整，不依赖其他示例状态
- ✅ 示例应覆盖常见的边界状态（如适用）

### 可用性检查
- ✅ 示例代码能够正常运行
- ✅ 引用的资源文件存在且可访问
- ✅ 示例数据具有代表性

## 文档审查清单

### 新增组件检查项
- [ ] 组件分类正确且目录结构一致
- [ ] 基础示例完整展示核心功能
- [ ] 示例描述与代码内容匹配
- [ ] API文档与源码完全一致
- [ ] CSS变量文档准确反映源码
- [ ] 资源文件引用路径正确且文件存在
- [ ] 特殊功能有对应的文档和示例

### 更新组件检查项
- [ ] 新功能有对应的示例和说明
- [ ] 已删除功能的文档已清理
- [ ] API变更已同步到文档
- [ ] 示例代码仍可正常运行
- [ ] 版本兼容性说明准确

---

# 🏆 最佳实践

## 文档编写原则
1. **清晰性**：使用清晰的语言描述组件功能
2. **完整性**：提供足够的示例代码
3. **实用性**：说明组件的使用场景
4. **准确性**：标注组件的限制和注意事项

## 示例设计原则
1. **场景覆盖**：示例应覆盖常见使用场景
2. **代码质量**：包含必要注释，避免复杂业务逻辑
3. **可复制性**：确保示例代码的可复制性
4. **场景覆盖**：根据组件特性覆盖相关使用场景

## API文档原则
1. **准确性**：属性说明具体且准确
2. **完整性**：提供类型定义和默认值
3. **限制说明**：说明属性的使用限制
4. **版本管理**：标注废弃的API
