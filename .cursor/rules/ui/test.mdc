---
description: '@v-md/ui' 的测试规范
globs: libs/ui/src/**/__tests__/*
alwaysApply: false
---
# 📋 概述

本文档定义了 '@v-md/ui' 组件库的测试代码编写、维护和质量控制标准。

---

# 📁 测试目录结构规范

## 测试文件组织结构
```
libs/ui/src/
├── component/                # 组件目录
│   ├── __tests__/           # 测试目录
│   │   ├── component.spec.tsx        # 主组件测试文件
│   │   ├── component-xxx.spec.tsx    # 子组件测试文件
│   │   ├── composables/            # 组合式函数测试目录
│   │   │   ├── a.spec.ts        # 组合式函数A测试文件
│   │   │   └── b.spec.ts        # 组合式函数B测试文件
│   │   └── fixtures/               # 测试夹具数据
│   │       ├── mock-data.ts        # 模拟数据
│   │       └── test-utils.ts       # 测试工具函数
│   ├── composables/         # 组合式函数
│   ├── types/              # 类型定义
│   ├── view/               # 组件视图
│   └── ...
└── ...
```

## 测试文件命名规范

### 基础命名规则
- **主组件测试**：`component.spec.tsx`（与主组件名称保持一致）
- **子组件测试**：`component-xxx.spec.tsx`（与子组件名称保持一致）
- **组合式函数测试**：`a.spec.ts` 或 `b.spec.ts` (与组合式函数名称保持一致)

---

# 🧪 测试框架和工具规范

## 核心测试框架
- **测试运行器**：Vitest
- **断言库**：Vitest 内置断言
- **Vue 测试工具**：@vue/test-utils，使用 JSX/TSX 语法编写组件
- **DOM 环境**：jsdom
- **模拟工具**：Vitest Mock Functions

---

# 📝 测试代码编写规范

## 测试用例命名规范

### 命名原则
- ✅ **必须使用英文**描述测试场景
- ✅ 采用 "should + 动作 + 预期结果" 的格式
- ✅ 测试描述要具体且明确
- ✅ 分组测试使用功能性描述
- ✅ 测试代码注释使用中文

### 命名示例
```typescript
describe('button', () => {
  describe('basic rendering', () => {
    it('should render correctly with default props', () => {
      // 应该正确渲染默认属性
    })
    it('should apply default style classes', () => {
      // 应该应用默认样式类
    })
    it('should render slot content when no text provided', () => {
      // 没有文本时应该渲染插槽内容
    })
  })

  describe('props', () => {
    it('should apply correct style class based on type prop', () => {
      // 应该根据 type 属性应用正确的样式类
    })
    it('should disable button when disabled is true', () => {
      // disabled 为 true 时应该禁用按钮
    })
    it('should show loading state when loading is true', () => {
      // loading 为 true 时应该显示加载状态
    })
  })

  describe('events', () => {
    it('should emit click event when clicked', () => {
      // 点击时应该触发 click 事件
    })
    it('should not emit click event when disabled', () => {
      // 禁用状态下不应该触发 click 事件
    })
    it('should pass correct event parameters', () => {
      // 应该传递正确的事件参数
    })
  })
})
```

---

# 🎯 测试覆盖范围规范

## 组件测试必测项目

### 1. 基础渲染测试
- ✅ 组件能够正常挂载
- ✅ 默认状态下的正确渲染
- ✅ 基础 DOM 结构正确
- ✅ 默认样式类正确应用

### 2. Props 测试
- ✅ 所有必填 props 的验证
- ✅ 可选 props 的默认值验证
- ✅ Props 类型验证（在可能的情况下）
- ✅ Props 变化时的响应式更新
- ✅ 边界值和异常值处理

### 3. Events 测试
- ✅ 所有自定义事件的触发条件
- ✅ 事件参数的正确性
- ✅ 事件触发时机的准确性
- ✅ 事件在特殊状态下的行为

### 4. Slots 测试
- ✅ 默认插槽的渲染
- ✅ 具名插槽的渲染
- ✅ 作用域插槽的数据传递
- ✅ 插槽回退内容的显示

### 5. 响应式数据测试
- ✅ v-model 双向绑定功能
- ✅ 响应式数据变化的正确处理
- ✅ 计算属性的正确计算
- ✅ 监听器的正确触发

### 6. 生命周期测试
- ✅ 组件挂载时的初始化逻辑
- ✅ 组件卸载时的清理逻辑
- ✅ 更新时的副作用处理

### 7. 用户交互测试
- ✅ 点击、键盘、鼠标等交互
- ✅ 表单输入和验证
- ✅ 拖拽、滚动等复杂交互
- ✅ 焦点管理和无障碍访问

## 组合式函数测试必测项目

### 1. 功能完整性测试
- ✅ 所有导出函数的基础功能
- ✅ 函数参数的正确处理
- ✅ 返回值的正确性
- ✅ 副作用的正确执行

### 2. 状态管理测试
- ✅ 响应式状态的初始化
- ✅ 状态变更的响应式更新
- ✅ 状态重置和清理
- ✅ 多实例状态隔离

### 3. 边界条件测试
- ✅ 异常参数的处理
- ✅ 空值和 undefined 的处理
- ✅ 极值条件的处理
- ✅ 错误状态的处理

---

# 🚀 测试性能和效率

## 测试运行优化

### 测试隔离策略
- ✅ 每个测试用例后清理 DOM
- ✅ 清理模拟函数状态
- ✅ 重置全局状态
- ✅ 及时卸载组件实例

### 测试用例组织
```typescript
// 合理使用 describe 分组
describe('ComponentName', () => {
  // 共享设置
  describe('基础功能', () => {
    // 相关测试用例
  })
  
  describe('高级功能', () => {
    // 相关测试用例
  })
})

// 避免过深嵌套
describe('功能模块', () => {
  it('应该处理场景1', () => {})
  it('应该处理场景2', () => {})
  it('应该处理场景3', () => {})
})
```

---

# ✅ 测试质量控制

## 代码审查检查项

### 测试代码质量
- [ ] 测试用例描述清晰明确
- [ ] 测试逻辑简单直接
- [ ] 断言明确且具体
- [ ] 避免测试用例间的相互依赖
- [ ] 合理使用模拟和夹具数据

### 测试覆盖性
- [ ] 覆盖主要功能路径
- [ ] 包含边界条件测试
- [ ] 包含异常情况处理
- [ ] 测试用户交互场景
- [ ] 验证无障碍性要求

### 测试维护性
- [ ] 测试代码遵循项目编码规范
- [ ] 合理复用测试工具和数据
- [ ] 及时更新过时的测试用例
- [ ] 文档化复杂测试逻辑

---

# 🏆 最佳实践

## 测试编写原则
- **简单性**：每个测试用例只测试一个功能点
- **独立性**：测试用例之间不应相互依赖
- **可读性**：测试代码应该易于理解和维护
- **稳定性**：测试结果应该可重复和可预测

## 测试策略
- **由外到内**：先测试组件的公共 API，再测试内部实现
- **用户视角**：从用户使用的角度编写测试用例
- **场景驱动**：基于实际使用场景设计测试

---

# 📚 参考资源

## 测试框架文档
- [Vitest 官方文档](https://vitest.dev/)
- [Vue Test Utils 文档](https://test-utils.vuejs.org/)
- [Testing Library 最佳实践](https://testing-library.com/docs/guiding-principles)

## 测试模式参考
- [Vue 组件测试模式](https://vuejs.org/guide/scaling-up/testing.html)
- [Jest 测试模式](https://jestjs.io/docs/testing-frameworks)
- [前端测试最佳实践](https://github.com/goldbergyoni/javascript-testing-best-practices)
